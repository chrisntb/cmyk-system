---
- name: Prompt for action
  ansible.builtin.pause:
    prompt: |
      Choose an action:
        logs (l)   - Show logs
        delete (d) - Delete
        exit (e)   - Exit
  register: _action_input
  until: _action_input.user_input | lower in ['logs', 'l', 'delete', 'd', 'exit', 'e']
  retries: 5
  delay: 0

- name: Wait for {{ resource_kind }} to be ready {{ actual_job_name }}
  ansible.builtin.command:
    cmd: >
      kubectl wait {{ resource_kind }}/{{ actual_job_name }}
      --for={{ 'condition=complete' if resource_kind == 'job' else 'condition=Ready' }}
      --timeout=30s
  environment:
    KUBECONFIG: "{{ ansible_env.HOME }}/.kube/config"
  register: _wait_result
  failed_when: false
  changed_when: false
  when: _action_input.user_input | lower in ['logs', 'l']

- name: Get logs {{ actual_job_name }}
  ansible.builtin.command:
    cmd: kubectl logs {{ resource_kind }}/{{ actual_job_name }}
  environment:
    KUBECONFIG: "{{ ansible_env.HOME }}/.kube/config"
  register: _logs_again
  changed_when: false
  when: _action_input.user_input | lower in ['logs', 'l']

- name: Print logs
  ansible.builtin.debug:
    msg: "{{ _logs_again.stdout_lines }}"
  when: _action_input.user_input | lower in ['logs', 'l']

- name: Delete {{ resource_kind }} {{ actual_job_name }}
  ansible.builtin.command:
    cmd: kubectl delete {{ resource_kind }} {{ actual_job_name }}
  environment:
    KUBECONFIG: "{{ ansible_env.HOME }}/.kube/config"
  changed_when: true
  when: _action_input.user_input | lower in ['delete', 'd']

- name: Return to action menu
  ansible.builtin.include_tasks: display_menu.yaml
  when: _action_input.user_input | lower in ['logs', 'l']
